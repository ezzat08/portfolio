---
import "../../styles/global.css";

const projectTitle = "EN Tuition Student Management System";
const projectType = "WEB APPLICATION · EDUCATION";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />

    <meta
      name="description"
      content="EN Tuition Student Management System — a web-based system developed using HTML, CSS, PHP, JavaScript and MySQL."
    />

    <meta
      name="keywords"
      content="EN Tuition, Student Management System, PHP, MySQL, HTML, CSS, JavaScript"
    />

    <meta
      name="author"
      content="Ahmad Ezzat Nuruddin"
    />

    <title>{projectTitle} — Ahmad Ezzat Nuruddin</title>
  </head>

  <body>
    <!-- SCROLL PROGRESS -->
    <div class="scroll-progress">
      <div class="scroll-progress-bar"></div>
    </div>

    <!-- CURSOR GLOW -->
    <div class="cursor-glow"></div>

    <main>
      <!-- =========================
           PROJECT HERO
           ========================= -->

      <section class="project-hero section-light">
        <div class="project-hero-decoration"></div>

        <div class="section-container">
          <!-- BACK TO PORTFOLIO -->
          <div class="project-back">
            <a href="/#projects">
              <span>←</span>
              Back to Portfolio
            </a>
          </div>

          <div class="project-hero-grid">
            <div class="project-hero-content">
              <p class="eyebrow project-eyebrow">
                {projectType}
              </p>

              <h1>
                EN Tuition
                <span>Student Management System.</span>
              </h1>

              <p class="project-hero-description">
                A web-based student management system developed for
                EN Tuition to manage students, tutors, learning
                materials, quizzes, results, and student queries.
              </p>

              <div class="project-hero-buttons">
                <a
                  href="#overview"
                  class="primary-button magnetic"
                >
                  Explore project
                  <span>↓</span>
                </a>

                <a
                  href="#demo"
                  class="secondary-button magnetic"
                >
                  Watch demo
                </a>
              </div>

              <div class="project-quick-info">
                <div>
                  <span>TYPE</span>
                  <p>Web Application</p>
                </div>

                <div>
                  <span>DATABASE</span>
                  <p>MySQL</p>
                </div>

                <div>
                  <span>STACK</span>
                  <p>PHP · JavaScript</p>
                </div>
              </div>
            </div>

            <div class="project-hero-preview interactive-tilt">
              <div class="preview-corner preview-corner-top"></div>
              <div class="preview-corner preview-corner-bottom"></div>

              <img
                src="/images/student-management.png"
                alt="EN Tuition Student Management System interface"
              />

              <div class="preview-label">
                SYSTEM INTERFACE
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- =========================
           OVERVIEW
           ========================= -->

      <section id="overview" class="section section-dark">
        <div class="section-container">
          <div class="section-heading">
            <p class="eyebrow">PROJECT OVERVIEW</p>

            <h2>
              Managing the{" "}
              <span>learning process.</span>
            </h2>

            <p>
              The system was developed to help EN Tuition manage
              student learning activities through one central
              web-based platform.
            </p>
          </div>

          <div class="overview-grid">
            <article class="overview-main interactive-card">
              <div class="overview-number">01</div>

              <div>
                <p class="overview-label">THE IDEA</p>

                <h3>
                  One system for students, tutors, and administration.
                </h3>

                <p>
                  EN Tuition Student Management System enables
                  different users to access functions based on
                  their role. Administrators can manage tutors and
                  view student information, while tutors can manage
                  learning content and student progress. Students
                  can take quizzes, view results, ask questions,
                  and access video lessons.
                </p>

                <p>
                  The system was designed as a practical web
                  application rather than a simple information
                  website, with database-driven features and
                  different levels of access for each user type.
                </p>
              </div>
            </article>

            <div class="overview-side">
              <article class="overview-stat interactive-card">
                <span>USERS</span>
                <strong>03</strong>
                <p>Admin · Tutor · Student</p>
              </article>

              <article class="overview-stat interactive-card">
                <span>CORE</span>
                <strong>05+</strong>
                <p>Learning and management functions</p>
              </article>

              <article class="overview-stat interactive-card">
                <span>DATABASE</span>
                <strong>SQL</strong>
                <p>MySQL database</p>
              </article>
            </div>
          </div>
        </div>
      </section>

      <!-- =========================
           TECHNOLOGY
           ========================= -->

      <section class="section section-light">
        <div class="section-container">
          <div class="section-heading">
            <p class="eyebrow">TECHNOLOGY</p>

            <h2>
              Built with {" "}
              <span>simple tools.</span>
            </h2>

            <p>
              The system used a straightforward web development
              stack suitable for a database-driven tuition
              management application.
            </p>
          </div>

          <div class="technology-grid">
            <article class="technology-card interactive-card">
              <span class="technology-number">01</span>

              <div class="technology-icon">
                HTML
              </div>

              <h3>HTML</h3>

              <p>
                Used to structure the pages, forms, navigation,
                tables, and system interface.
              </p>

              <span class="technology-type">
                FRONTEND
              </span>
            </article>

            <article class="technology-card interactive-card">
              <span class="technology-number">02</span>

              <div class="technology-icon">
                CSS
              </div>

              <h3>CSS</h3>

              <p>
                Used to design the interface, layouts, forms,
                navigation elements, and visual presentation.
              </p>

              <span class="technology-type">
                FRONTEND
              </span>
            </article>

            <article class="technology-card interactive-card">
              <span class="technology-number">03</span>

              <div class="technology-icon">
                JS
              </div>

              <h3>JavaScript</h3>

              <p>
                Used for selected client-side interactions and
                dynamic behaviour within the system.
              </p>

              <span class="technology-type">
                FRONTEND
              </span>
            </article>

            <article class="technology-card interactive-card">
              <span class="technology-number">04</span>

              <div class="technology-icon">
                PHP
              </div>

              <h3>PHP</h3>

              <p>
                Used for the server-side application logic,
                processing requests, and connecting the system
                with the database.
              </p>

              <span class="technology-type">
                BACKEND
              </span>
            </article>

            <article class="technology-card interactive-card">
              <span class="technology-number">05</span>

              <div class="technology-icon">
                SQL
              </div>

              <h3>MySQL</h3>

              <p>
                Used to store and manage student, tutor, quiz,
                result, query, and learning content data.
              </p>

              <span class="technology-type">
                DATABASE
              </span>
            </article>
          </div>
        </div>
      </section>

      <!-- =========================
           USER ROLES
           ========================= -->

      <section class="section section-dark">
        <div class="section-container">
          <div class="section-heading">
            <p class="eyebrow">SYSTEM ROLES</p>

            <h2>
              Three users,{" "}
              <span>different access.</span>
            </h2>

            <p>
              The system separates its functions according to the
              user's role, allowing each type of user to access
              the features they need.
            </p>
          </div>

          <div class="roles-grid">
            <article class="role-card interactive-card">
              <div class="role-top">
                <span class="role-number">01</span>

                <span class="role-type">
                  MANAGEMENT
                </span>
              </div>

              <div class="role-icon">
                ADM
              </div>

              <h3>Admin</h3>

              <p>
                Responsible for viewing student information and
                managing tutor accounts.
              </p>

              <div class="role-divider"></div>

              <div class="role-functions">
                <span>Student Detail</span>
                <span>Tutor Detail</span>
              </div>
            </article>

            <article class="role-card interactive-card">
              <div class="role-top">
                <span class="role-number">02</span>

                <span class="role-type">
                  LEARNING
                </span>
              </div>

              <div class="role-icon">
                TUT
              </div>

              <h3>Tutor</h3>

              <p>
                Manages students, quizzes, results, questions, and
                video lessons.
              </p>

              <div class="role-divider"></div>

              <div class="role-functions">
                <span>Student Detail</span>
                <span>Quiz</span>
                <span>Publish Result</span>
                <span>Edit Result</span>
                <span>Student Query</span>
                <span>Video</span>
              </div>
            </article>

            <article class="role-card interactive-card">
              <div class="role-top">
                <span class="role-number">03</span>

                <span class="role-type">
                  LEARNER
                </span>
              </div>

              <div class="role-icon">
                STU
              </div>

              <h3>Student</h3>

              <p>
                Uses the platform to access learning materials,
                complete quizzes, view results, and communicate
                with tutors.
              </p>

              <div class="role-divider"></div>

              <div class="role-functions">
                <span>Take Quiz</span>
                <span>View Result</span>
                <span>Ask Query</span>
                <span>View Query</span>
                <span>Video</span>
              </div>
            </article>
          </div>
        </div>
      </section>

      <!-- =========================
           FUNCTION BREAKDOWN
           ========================= -->

      <section class="section section-light">
        <div class="section-container">
          <div class="section-heading">
            <p class="eyebrow">FEATURES</p>

            <h2>
              From enrolment{" "}
              <span>to learning.</span>
            </h2>

            <p>
              The system covers the main activities required to
              manage students and support their learning progress.
            </p>
          </div>

          <div class="feature-list">
            <!-- ADMIN -->
            <div class="feature-group">
              <div class="feature-group-heading">
                <span>01</span>

                <div>
                  <p>ADMIN</p>
                  <h3>System management</h3>
                </div>
              </div>

              <div class="feature-items">
                <article class="feature-item interactive-card">
                  <span>01</span>

                  <div>
                    <h4>Student Detail</h4>

                    <p>
                      Allows administrators to view student
                      information stored in the system.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>02</span>

                  <div>
                    <h4>Tutor Detail</h4>

                    <p>
                      Allows administrators to add, update, and
                      delete tutor records.
                    </p>
                  </div>
                </article>
              </div>
            </div>

            <!-- TUTOR -->
            <div class="feature-group">
              <div class="feature-group-heading">
                <span>02</span>

                <div>
                  <p>TUTOR</p>
                  <h3>Learning management</h3>
                </div>
              </div>

              <div class="feature-items">
                <article class="feature-item interactive-card">
                  <span>01</span>

                  <div>
                    <h4>Student Detail</h4>

                    <p>
                      Tutors can add, update, and delete student
                      records.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>02</span>

                  <div>
                    <h4>Quiz Section</h4>

                    <p>
                      Tutors can upload, update, and delete
                      quizzes for students.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>03</span>

                  <div>
                    <h4>Publish Result</h4>

                    <p>
                      Tutors can review student quiz answers and
                      mark their results.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>04</span>

                  <div>
                    <h4>Edit Result</h4>

                    <p>
                      Tutors can update or delete previously
                      recorded quiz results.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>05</span>

                  <div>
                    <h4>Student Query</h4>

                    <p>
                      Tutors can view questions submitted by
                      students.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>06</span>

                  <div>
                    <h4>Video</h4>

                    <p>
                      Tutors can add, update, and delete video
                      lessons for students.
                    </p>
                  </div>
                </article>
              </div>
            </div>

            <!-- STUDENT -->
            <div class="feature-group">
              <div class="feature-group-heading">
                <span>03</span>

                <div>
                  <p>STUDENT</p>
                  <h3>Learning activities</h3>
                </div>
              </div>

              <div class="feature-items">
                <article class="feature-item interactive-card">
                  <span>01</span>

                  <div>
                    <h4>Take Quiz</h4>

                    <p>
                      Students can access and answer available
                      quizzes.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>02</span>

                  <div>
                    <h4>View Result</h4>

                    <p>
                      Students can view their quiz results.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>03</span>

                  <div>
                    <h4>Ask Query</h4>

                    <p>
                      Students can send questions to their tutors.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>04</span>

                  <div>
                    <h4>View Query</h4>

                    <p>
                      Students can view answers provided by their
                      tutors.
                    </p>
                  </div>
                </article>

                <article class="feature-item interactive-card">
                  <span>05</span>

                  <div>
                    <h4>Video</h4>

                    <p>
                      Students can access available video lessons.
                    </p>
                  </div>
                </article>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- =========================
           INTERFACE
           ========================= -->

      <section id="interface" class="section section-dark">
        <div class="section-container">
          <div class="section-heading">
            <p class="eyebrow">SYSTEM INTERFACE</p>

            <h2>
              Designed around{" "}
              <span>the user.</span>
            </h2>

            <p>
              The interface provides different screens and
              functions depending on the user's role.
            </p>
          </div>

          <div class="interface-showcase">
            <div class="interface-main interactive-card">
              <div class="interface-image">
                <img
                  src="/images/student-management.png"
                  alt="EN Tuition Student Management System dashboard"
                />

                <div class="interface-overlay">
                  <span>
                    EN TUITION SMS
                  </span>
                </div>
              </div>

              <div class="interface-caption">
                <div>
                  <span>MAIN INTERFACE</span>

                  <h3>
                    Student Management System
                  </h3>
                </div>

                <p>
                  Database-driven web interface
                </p>
              </div>
            </div>

            <div class="interface-side">
              <div class="interface-side-item">
                <span>ADMIN</span>

                <p>
                  Student and tutor management
                </p>
              </div>

              <div class="interface-side-item">
                <span>TUTOR</span>

                <p>
                  Quiz, result, query, and video management
                </p>
              </div>

              <div class="interface-side-item">
                <span>STUDENT</span>

                <p>
                  Quiz, result, query, and video access
                </p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- =========================
           DEMO VIDEO
           ========================= -->

      <section id="demo" class="section section-light">
        <div class="section-container">
          <div class="section-heading">
            <p class="eyebrow">DEMO</p>

            <h2>
              See it{" "}
              <span>in action.</span>
            </h2>

            <p>
              A walkthrough of the EN Tuition Student Management
              System and its main functions.
            </p>
          </div>

          <div class="video-wrapper">
            <div class="video-frame">
              <div class="video-topbar">
                <div class="video-dots">
                  <span></span>
                  <span></span>
                  <span></span>
                </div>

                <span>
                  EN TUITION · SYSTEM DEMO
                </span>

                <span>
                  DEMO
                </span>
              </div>

              <video
                controls
                preload="metadata"
                poster="/images/student-management.png"
              >
                <source
                  src="/videos/student-management-demo.mp4"
                  type="video/mp4"
                />

                Your browser does not support the video element.
              </video>
            </div>

            <div class="video-caption">
              <span>PROJECT DEMONSTRATION</span>

              <p>
                EN Tuition Student Management System
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- =========================
           SYSTEM FLOW
           ========================= -->

      <section class="section section-dark">
        <div class="section-container">
          <div class="section-heading">
            <p class="eyebrow">SYSTEM FLOW</p>

            <h2>
              Connecting
              <span>the users.</span>
            </h2>

            <p>
              Each role interacts with the same system through
              different functions and permissions.
            </p>
          </div>

          <div class="flow">
            <div class="flow-line"></div>

            <article class="flow-step interactive-card">
              <span>01</span>

              <div class="flow-icon">
                ADM
              </div>

              <h3>Admin</h3>

              <p>
                Manages tutor information and views student
                details.
              </p>
            </article>

            <div class="flow-connector">
              →
            </div>

            <article class="flow-step interactive-card">
              <span>02</span>

              <div class="flow-icon">
                TUT
              </div>

              <h3>Tutor</h3>

              <p>
                Manages students, quizzes, results, queries, and
                video lessons.
              </p>
            </article>

            <div class="flow-connector">
              →
            </div>

            <article class="flow-step interactive-card">
              <span>03</span>

              <div class="flow-icon">
                STU
              </div>

              <h3>Student</h3>

              <p>
                Uses the learning features and communicates with
                tutors through the system.
              </p>
            </article>
          </div>
        </div>
      </section>

      <!-- =========================
           PROJECT TAKEAWAY
           ========================= -->

      <section class="section section-light">
        <div class="section-container">
          <div class="takeaway">
            <div class="takeaway-number">
              01
            </div>

            <div class="takeaway-content">
              <p class="eyebrow">
                PROJECT TAKEAWAY
              </p>

              <h2>
                A practical database-driven
                <span>web application.</span>
              </h2>

              <p>
                This project gave me practical experience in
                developing a complete web-based system where the
                frontend, backend, database, and user functions
                work together.
              </p>

              <p>
                Instead of focusing only on the interface, the
                project involved managing different user roles,
                processing information, storing records, and
                building features around the actual workflow of a
                tuition centre.
              </p>

              <div class="takeaway-tags">
                <span>Web Development</span>
                <span>PHP</span>
                <span>MySQL</span>
                <span>CRUD</span>
                <span>Database</span>
                <span>User Roles</span>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- =========================
           PROJECT INFO
           ========================= -->

      <section class="project-info-section section-dark">
        <div class="section-container">
          <div class="project-info-grid">
            <div>
              <span>PROJECT</span>

              <h3>
                EN Tuition Student Management System
              </h3>
            </div>

            <div>
              <span>TYPE</span>

              <p>
                Web Application
              </p>
            </div>

            <div>
              <span>TECHNOLOGY</span>

              <p>
                HTML · CSS · JavaScript · PHP · MySQL
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- =========================
           PROJECT END
           ========================= -->

      <section class="project-end section-light">
        <div class="section-container">
          <p class="eyebrow">
            MORE PROJECTS
          </p>

          <h2>
            Back to
            <span>my work.</span>
          </h2>

          <a
            href="/#projects"
            class="primary-button magnetic"
          >
            View all projects
            <span>↗</span>
          </a>
        </div>
      </section>
    </main>

    <!-- FOOTER -->
    <footer class="footer">
      <div class="footer-container">
        <span>
          © {new Date().getFullYear()} Ahmad Ezzat Nuruddin
        </span>

        <span>
          Software · IoT · Systems
        </span>
      </div>
    </footer>

    <script>
      /* =========================
         SCROLL PROGRESS
         ========================= */

      const progressBar =
        document.querySelector<HTMLElement>(
          ".scroll-progress-bar"
        );

      const updateProgress = () => {
        if (!progressBar) return;

        const scrollTop = window.scrollY;

        const documentHeight =
          document.documentElement.scrollHeight -
          window.innerHeight;

        const progress =
          documentHeight > 0
            ? (scrollTop / documentHeight) * 100
            : 0;

        progressBar.style.width = `${progress}%`;
      };

      window.addEventListener(
        "scroll",
        updateProgress,
        {
          passive: true
        }
      );

      updateProgress();

      /* =========================
         CURSOR GLOW
         ========================= */

      const cursorGlow =
        document.querySelector<HTMLElement>(
          ".cursor-glow"
        );

      if (
        cursorGlow &&
        window.matchMedia("(pointer: fine)").matches
      ) {
        window.addEventListener(
          "pointermove",
          (event) => {
            cursorGlow.style.transform =
              `translate3d(${event.clientX}px, ${event.clientY}px, 0) translate(-50%, -50%)`;
          }
        );
      }

      /* =========================
         REVEAL ANIMATIONS
         ========================= */

      const revealItems =
        document.querySelectorAll<HTMLElement>(
          ".section-heading, .overview-main, .overview-stat, .technology-card, .role-card, .feature-group, .feature-item, .interface-main, .interface-side-item, .video-wrapper, .flow-step, .takeaway, .project-info-grid, .project-end"
        );

      const revealObserver =
        new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (!entry.isIntersecting) return;

              entry.target.classList.add(
                "visible"
              );

              revealObserver.unobserve(
                entry.target
              );
            });
          },
          {
            threshold: 0.12
          }
        );

      revealItems.forEach((item) => {
        item.classList.add("reveal");
        revealObserver.observe(item);
      });

      /* =========================
         INTERACTIVE CARDS
         ========================= */

      const interactiveCards =
        document.querySelectorAll<HTMLElement>(
          ".interactive-card"
        );

      if (
        window.matchMedia("(pointer: fine)").matches
      ) {
        interactiveCards.forEach((card) => {
          card.addEventListener(
            "mousemove",
            (event: MouseEvent) => {
              const rect =
                card.getBoundingClientRect();

              const x =
                event.clientX - rect.left;

              const y =
                event.clientY - rect.top;

              const percentX =
                (x / rect.width) * 100;

              const percentY =
                (y / rect.height) * 100;

              card.style.setProperty(
                "--mouse-x",
                `${percentX}%`
              );

              card.style.setProperty(
                "--mouse-y",
                `${percentY}%`
              );
            }
          );

          card.addEventListener(
            "mouseleave",
            () => {
              card.style.setProperty(
                "--mouse-x",
                "50%"
              );

              card.style.setProperty(
                "--mouse-y",
                "50%"
              );
            }
          );
        });
      }

      /* =========================
         MAGNETIC BUTTONS
         ========================= */

      const magneticElements =
        document.querySelectorAll<HTMLElement>(
          ".magnetic"
        );

      if (
        window.matchMedia("(pointer: fine)").matches
      ) {
        magneticElements.forEach(
          (element) => {
            element.addEventListener(
              "mousemove",
              (event: MouseEvent) => {
                const rect =
                  element.getBoundingClientRect();

                const x =
                  event.clientX -
                  rect.left -
                  rect.width / 2;

                const y =
                  event.clientY -
                  rect.top -
                  rect.height / 2;

                element.style.transform =
                  `translate(${x * 0.08}px, ${y * 0.08}px)`;
              }
            );

            element.addEventListener(
              "mouseleave",
              () => {
                element.style.transform =
                  "";
              }
            );
          }
        );
      }

      /* =========================
         HERO IMAGE TILT
         ========================= */

      const projectPreview =
        document.querySelector<HTMLElement>(
          ".interactive-tilt"
        );

      if (
        projectPreview &&
        window.matchMedia("(pointer: fine)").matches
      ) {
        projectPreview.addEventListener(
          "mousemove",
          (event: MouseEvent) => {
            const rect =
              projectPreview.getBoundingClientRect();

            const x =
              event.clientX - rect.left;

            const y =
              event.clientY - rect.top;

            const rotateX =
              ((y / rect.height) - 0.5) * -5;

            const rotateY =
              ((x / rect.width) - 0.5) * 5;

            projectPreview.style.transform =
              `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateY(-5px)`;
          }
        );

        projectPreview.addEventListener(
          "mouseleave",
          () => {
            projectPreview.style.transform =
              "";
          }
        );
      }

      /* =========================
         SMOOTH SCROLLING
         ========================= */

      document
        .querySelectorAll<HTMLAnchorElement>(
          'a[href^="#"]'
        )
        .forEach((link) => {
          link.addEventListener(
            "click",
            (event) => {
              const targetId =
                link.getAttribute("href");

              if (
                !targetId ||
                targetId === "#"
              ) {
                return;
              }

              const target =
                document.querySelector(
                  targetId
                );

              if (!target) return;

              event.preventDefault();

              target.scrollIntoView({
                behavior: "smooth",
                block: "start"
              });
            }
          );
        });
    </script>
  </body>
</html>

<style>
  /* =========================
     BASE
     ========================= */

  :global(*) {
    box-sizing: border-box;
  }

  :global(html) {
    scroll-behavior: smooth;
  }

  :global(body) {
    margin: 0;
    font-family:
      Inter,
      ui-sans-serif,
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      sans-serif;
    background: #f4efe6;
    color: #29251f;
    font-size: 15px;
    line-height: 1.6;
    overflow-x: hidden;
  }

  :global(a) {
    color: inherit;
    text-decoration: none;
  }

  :global(img) {
    display: block;
    max-width: 100%;
  }

  :global(video) {
    display: block;
    max-width: 100%;
  }

  /* =========================
     SCROLL PROGRESS
     ========================= */

  .scroll-progress {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    z-index: 2000;
    pointer-events: none;
  }

  .scroll-progress-bar {
    width: 0;
    height: 100%;
    background: #9b7653;
    transition: width 0.05s linear;
  }

  /* =========================
     CURSOR GLOW
     ========================= */

  .cursor-glow {
    position: fixed;
    top: 0;
    left: 0;
    width: 260px;
    height: 260px;
    border-radius: 50%;
    pointer-events: none;
    z-index: 0;
    background: radial-gradient(
      circle,
      rgba(155, 118, 83, 0.09) 0%,
      rgba(155, 118, 83, 0) 70%
    );
    transform:
      translate3d(-500px, -500px, 0)
      translate(-50%, -50%);
  }

  /* =========================
     GENERAL SECTIONS
     ========================= */

  .section {
    position: relative;
    padding: 120px 0;
    overflow: hidden;
  }

  .section-light {
    background: #f4efe6;
  }

  .section-dark {
    background: #29251f;
    color: #f4efe6;
  }

  .section-container {
    position: relative;
    z-index: 2;
    width:
      min(1180px, calc(100% - 64px));
    margin: 0 auto;
  }

  .section-heading {
    max-width: 700px;
    margin-bottom: 70px;
  }

  .eyebrow {
    margin: 0 0 16px;
    color: #806344;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.16em;
  }

  .section-dark .eyebrow {
    color: #b99a77;
  }

  .section-heading h2 {
    position: relative;
    display: inline-block;
    margin: 0 0 20px;
    font-size: clamp(40px, 5vw, 62px);
    line-height: 1.05;
    letter-spacing: -0.05em;
    font-weight: 600;
  }

  .section-heading h2::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -8px;
    width: 45px;
    height: 2px;
    background: #9b7653;
    transform: scaleX(0);
    transform-origin: left;
    transition:
      transform 0.8s ease 0.2s;
  }

  .section-heading.visible h2::after {
    transform: scaleX(1);
  }

  .section-heading h2 span {
    display: inline;
    color: #a38b70;
  }

  .section-heading > p:last-child {
    max-width: 590px;
    margin: 0;
    color: #625a50;
    font-size: 15px;
    line-height: 1.7;
  }

  .section-dark
    .section-heading
    > p:last-child {
    color: #b9b0a3;
  }

  /* =========================
     PROJECT HERO
     ========================= */

  .project-hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 110px 0 100px;
    overflow: hidden;
  }

  .project-hero-decoration {
    position: absolute;
    top: 20%;
    right: -160px;
    width: 500px;
    height: 500px;
    border: 1px solid
      rgba(155, 118, 83, 0.1);
    border-radius: 50%;
  }

  .project-hero-decoration::before,
  .project-hero-decoration::after {
    content: "";
    position: absolute;
    inset: 55px;
    border: 1px solid
      rgba(155, 118, 83, 0.08);
    border-radius: 50%;
  }

  .project-hero-decoration::after {
    inset: 115px;
  }

  /* =========================
     BACK TO PORTFOLIO
     ========================= */

  .project-back {
    position: relative;
    z-index: 3;
    display: block;
    width: fit-content;
    margin: 0 0 45px 0;
    padding: 0;
  }

  .project-back a {
    display: inline-flex !important;
    align-items: center;
    gap: 7px;
    width: fit-content !important;
    max-width: fit-content !important;
    padding: 0 !important;
    margin: 0 !important;
    background: transparent !important;
    border: 0 !important;
    box-shadow: none !important;
    color: #806344;
    font-size: 10px;
    font-weight: 600;
    line-height: 1.4;
    letter-spacing: 0.04em;
    text-transform: none;
    transition:
      color 0.2s ease,
      transform 0.2s ease;
  }

  .project-back a span {
    display: inline-block;
    font-size: 15px;
    line-height: 1;
    transition:
      transform 0.2s ease;
  }

  .project-back a:hover {
    color: #9b7653;
    transform: translateX(-3px);
  }

  .project-back a:hover span {
    transform: translateX(-3px);
  }

  .project-hero-grid {
    display: grid;
    grid-template-columns:
      minmax(0, 1fr)
      minmax(350px, 0.85fr);
    gap: 90px;
    align-items: center;
  }

  .project-eyebrow {
    animation:
      projectFade 0.8s ease both;
  }

  .project-hero-content h1 {
    max-width: 780px;
    margin: 0 0 30px;
    font-size:
      clamp(54px, 7vw, 88px);
    line-height: 0.96;
    letter-spacing: -0.07em;
    font-weight: 600;
    animation:
      projectTitle 0.9s ease both;
  }

  .project-hero-content h1 span {
    display: block;
    color: #9b7653;
  }

  .project-hero-description {
    max-width: 650px;
    margin: 0;
    color: #625a50;
    font-size: 17px;
    line-height: 1.7;
    animation:
      projectFade 0.8s ease 0.2s both;
  }

  .project-hero-buttons {
    display: flex;
    align-items: center;
    gap: 14px;
    margin-top: 38px;
    animation:
      projectFade 0.8s ease 0.3s both;
  }

  .primary-button,
  .secondary-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 13px 20px;
    font-size: 13px;
    font-weight: 600;
    transition:
      transform 0.2s ease,
      background 0.2s ease,
      color 0.2s ease,
      border-color 0.2s ease;
  }

  .primary-button {
    background: #29251f;
    color: #f4efe6;
  }

  .primary-button:hover {
    background: #9b7653;
  }

  .primary-button span {
    transition:
      transform 0.25s ease;
  }

  .primary-button:hover span {
    transform: translateY(3px);
  }

  .secondary-button {
    border: 1px solid #b9ad9e;
    color: #40382f;
  }

  .secondary-button:hover {
    border-color: #9b7653;
    background: #e9e0d3;
  }

  .project-quick-info {
    display: flex;
    gap: 45px;
    margin-top: 55px;
    padding-top: 25px;
    border-top: 1px solid #d4c8b9;
    animation:
      projectFade 0.8s ease 0.4s both;
  }

  .project-quick-info div {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .project-quick-info span {
    color: #806344;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.13em;
  }

  .project-quick-info p {
    margin: 0;
    color: #625a50;
    font-size: 12px;
  }

  .project-hero-preview {
    position: relative;
    padding: 12px;
    border: 1px solid #cfc3b4;
    background: #e9e0d3;
    transform-style: preserve-3d;
    animation:
      projectPreview 1s ease 0.2s both;
    transition:
      transform 0.15s ease-out,
      box-shadow 0.3s ease;
  }

  .project-hero-preview:hover {
    box-shadow:
      0 25px 55px
        rgba(41, 37, 31, 0.16);
  }

  .project-hero-preview img {
    width: 100%;
    aspect-ratio: 16 / 10;
    object-fit: cover;
    filter: saturate(0.85);
  }

  .preview-corner {
    position: absolute;
    width: 38px;
    height: 38px;
    z-index: 3;
  }

  .preview-corner-top {
    top: -7px;
    right: -7px;
    border-top: 2px solid #9b7653;
    border-right: 2px solid #9b7653;
  }

  .preview-corner-bottom {
    bottom: -7px;
    left: -7px;
    border-bottom: 2px solid #9b7653;
    border-left: 2px solid #9b7653;
  }

  .preview-label {
    position: absolute;
    left: 28px;
    bottom: 28px;
    padding: 7px 10px;
    background: rgba(41, 37, 31, 0.92);
    color: #f4efe6;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.1em;
  }

  @keyframes projectFade {
    from {
      opacity: 0;
      transform: translateY(20px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes projectTitle {
    from {
      opacity: 0;
      transform: translateY(35px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes projectPreview {
    from {
      opacity: 0;
      transform:
        translateY(35px)
        scale(0.96)
        rotate(1deg);
    }

    to {
      opacity: 1;
      transform:
        translateY(0)
        scale(1)
        rotate(0);
    }
  }

  /* =========================
     OVERVIEW
     ========================= */

  .overview-grid {
    display: grid;
    grid-template-columns: 1.4fr 0.6fr;
    gap: 25px;
  }

  .overview-main {
    position: relative;
    display: grid;
    grid-template-columns: 55px 1fr;
    gap: 30px;
    min-height: 420px;
    padding: 40px;
    overflow: hidden;
    border: 1px solid
      rgba(244, 239, 230, 0.14);
    background: #322e27;
  }

  .overview-number {
    position: relative;
    z-index: 2;
    color: #9b7653;
    font-size: 11px;
    font-weight: 700;
  }

  .overview-main > div:last-child {
    position: relative;
    z-index: 2;
  }

  .overview-label {
    margin: 0 0 15px;
    color: #b99a77;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .overview-main h3 {
    max-width: 650px;
    margin: 0 0 25px;
    font-size: 32px;
    line-height: 1.2;
    letter-spacing: -0.04em;
    font-weight: 600;
  }

  .overview-main p:not(.overview-label) {
    max-width: 680px;
    margin: 0 0 18px;
    color: #b9b0a3;
    font-size: 14px;
    line-height: 1.75;
  }

  .overview-side {
    display: grid;
    gap: 1px;
    background: rgba(185, 154, 119, 0.2);
  }

  .overview-stat {
    position: relative;
    padding: 30px;
    background: #322e27;
    overflow: hidden;
  }

  .overview-stat span {
    display: block;
    margin-bottom: 8px;
    color: #b99a77;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .overview-stat strong {
    display: block;
    color: #f4efe6;
    font-size: 35px;
    line-height: 1;
    font-weight: 600;
    letter-spacing: -0.05em;
  }

  .overview-stat p {
    margin: 9px 0 0;
    color: #968d81;
    font-size: 11px;
  }

  /* =========================
     TECHNOLOGY
     ========================= */

  .technology-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1px;
    background: #d4c8b9;
    border: 1px solid #d4c8b9;
  }

  .technology-card {
    position: relative;
    min-height: 330px;
    padding: 28px;
    overflow: hidden;
    background: #f4efe6;
    transition:
      transform 0.3s ease,
      background 0.3s ease,
      box-shadow 0.3s ease;
  }

  .technology-card:hover {
    transform: translateY(-7px);
    background: #eee6da;
    box-shadow:
      0 20px 45px
        rgba(41, 37, 31, 0.1);
  }

  .technology-number {
    position: relative;
    z-index: 2;
    color: #9b7653;
    font-size: 10px;
    font-weight: 700;
  }

  .technology-icon {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 64px;
    height: 64px;
    margin: 35px 0 25px;
    border: 1px solid #cdbba7;
    border-radius: 14px;
    background: #eee6da;
    color: #806344;
    font-family: monospace;
    font-size: 12px;
    font-weight: 700;
    transition:
      transform 0.3s ease,
      border-color 0.3s ease;
  }

  .technology-card:hover .technology-icon {
    transform:
      translateY(-4px)
      rotate(-3deg);
    border-color: #9b7653;
  }

  .technology-card h3 {
    position: relative;
    z-index: 2;
    margin: 0 0 12px;
    font-size: 18px;
    line-height: 1.3;
    font-weight: 600;
  }

  .technology-card p {
    position: relative;
    z-index: 2;
    margin: 0;
    color: #625a50;
    font-size: 12px;
    line-height: 1.65;
  }

  .technology-type {
    position: absolute;
    right: 28px;
    bottom: 28px;
    color: #9b8c7b;
    font-size: 8px;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  /* =========================
     ROLES
     ========================= */

  .roles-grid {
    display: grid;
    grid-template-columns:
      repeat(3, 1fr);
    gap: 1px;
    background: rgba(185, 154, 119, 0.2);
  }

  .role-card {
    position: relative;
    min-height: 450px;
    padding: 35px;
    overflow: hidden;
    background: #322e27;
    transition:
      transform 0.3s ease,
      background 0.3s ease,
      box-shadow 0.3s ease;
  }

  .role-card:hover {
    transform: translateY(-8px);
    background: #373229;
    box-shadow:
      0 25px 50px
        rgba(0, 0, 0, 0.16);
  }

  .role-top {
    position: relative;
    z-index: 2;
    display: flex;
    justify-content: space-between;
    gap: 20px;
  }

  .role-number {
    color: #9b7653;
    font-size: 11px;
    font-weight: 700;
  }

  .role-type {
    color: #8f877c;
    font-size: 8px;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .role-icon {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 75px;
    height: 75px;
    margin: 45px 0 30px;
    border: 1px solid
      rgba(185, 154, 119, 0.35);
    border-radius: 18px;
    color: #b99a77;
    font-family: monospace;
    font-size: 12px;
    font-weight: 700;
    transition:
      transform 0.3s ease,
      background 0.3s ease;
  }

  .role-card:hover .role-icon {
    transform:
      translateY(-5px)
      rotate(-3deg);
    background:
      rgba(155, 118, 83, 0.08);
  }

  .role-card h3 {
    position: relative;
    z-index: 2;
    margin: 0 0 13px;
    font-size: 24px;
    font-weight: 600;
  }

  .role-card > p {
    position: relative;
    z-index: 2;
    max-width: 330px;
    margin: 0;
    color: #b9b0a3;
    font-size: 13px;
    line-height: 1.7;
  }

  .role-divider {
    position: absolute;
    left: 35px;
    right: 35px;
    bottom: 105px;
    height: 1px;
    background:
      rgba(244, 239, 230, 0.1);
  }

  .role-functions {
    position: absolute;
    left: 35px;
    right: 35px;
    bottom: 35px;
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
  }

  .role-functions span {
    padding: 5px 8px;
    border: 1px solid
      rgba(185, 154, 119, 0.22);
    color: #a9a095;
    font-size: 9px;
  }

  /* =========================
     FEATURES
     ========================= */

  .feature-list {
    display: grid;
    gap: 80px;
  }

  .feature-group {
    display: grid;
    grid-template-columns:
      280px 1fr;
    gap: 70px;
  }

  .feature-group-heading {
    display: grid;
    grid-template-columns: 40px 1fr;
    gap: 20px;
    align-items: start;
  }

  .feature-group-heading > span {
    color: #9b7653;
    font-size: 11px;
    font-weight: 700;
  }

  .feature-group-heading p {
    margin: 0 0 8px;
    color: #806344;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.13em;
  }

  .feature-group-heading h3 {
    margin: 0;
    font-size: 25px;
    line-height: 1.2;
    letter-spacing: -0.03em;
    font-weight: 600;
  }

  .feature-items {
    display: grid;
    grid-template-columns:
      repeat(2, 1fr);
    gap: 12px;
  }

  .feature-item {
    position: relative;
    display: grid;
    grid-template-columns:
      30px 1fr;
    gap: 15px;
    min-height: 150px;
    padding: 23px;
    overflow: hidden;
    border: 1px solid #d4c8b9;
    background: #eee6da;
    transition:
      transform 0.3s ease,
      border-color 0.3s ease,
      box-shadow 0.3s ease;
  }

  .feature-item:hover {
    transform: translateY(-5px);
    border-color: #b99a77;
    box-shadow:
      0 15px 35px
        rgba(41, 37, 31, 0.08);
  }

  .feature-item > span {
    color: #9b7653;
    font-size: 9px;
    font-weight: 700;
  }

  .feature-item h4 {
    margin: 0 0 7px;
    font-size: 15px;
    font-weight: 600;
  }

  .feature-item p {
    margin: 0;
    color: #625a50;
    font-size: 11px;
    line-height: 1.65;
  }

  /* =========================
     INTERFACE
     ========================= */

  .interface-showcase {
    display: grid;
    grid-template-columns:
      1fr 280px;
    gap: 25px;
  }

  .interface-main {
    position: relative;
    overflow: hidden;
    border: 1px solid
      rgba(244, 239, 230, 0.15);
    background: #322e27;
  }

  .interface-image {
    position: relative;
    overflow: hidden;
    background: #201d18;
  }

  .interface-image img {
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    transition:
      transform 0.7s ease,
      filter 0.5s ease;
  }

  .interface-main:hover
    .interface-image img {
    transform: scale(1.03);
    filter: brightness(0.8);
  }

  .interface-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: flex-end;
    padding: 25px;
    background:
      linear-gradient(
        to top,
        rgba(41, 37, 31, 0.7),
        transparent 45%
      );
  }

  .interface-overlay span {
    padding: 7px 10px;
    background: rgba(41, 37, 31, 0.9);
    color: #f4efe6;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.1em;
  }

  .interface-caption {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 25px;
    padding: 25px;
  }

  .interface-caption span {
    color: #b99a77;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .interface-caption h3 {
    margin: 7px 0 0;
    font-size: 20px;
    font-weight: 600;
  }

  .interface-caption p {
    margin: 0;
    color: #91887b;
    font-size: 10px;
  }

  .interface-side {
    display: grid;
    grid-template-rows:
      repeat(3, 1fr);
    gap: 1px;
    background:
      rgba(185, 154, 119, 0.2);
  }

  .interface-side-item {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 30px;
    background: #322e27;
    transition:
      transform 0.3s ease,
      background 0.3s ease;
  }

  .interface-side-item:hover {
    transform: translateX(-5px);
    background: #373229;
  }

  .interface-side-item span {
    color: #9b7653;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .interface-side-item p {
    margin: 10px 0 0;
    color: #b9b0a3;
    font-size: 12px;
    line-height: 1.55;
  }

  /* =========================
     VIDEO
     ========================= */

  .video-wrapper {
    max-width: 1050px;
    margin: 0 auto;
  }

  .video-frame {
    position: relative;
    padding: 10px;
    border: 1px solid #cfc3b4;
    background: #e9e0d3;
    box-shadow:
      0 20px 50px
        rgba(41, 37, 31, 0.08);
  }

  .video-topbar {
    display: grid;
    grid-template-columns:
      1fr auto 1fr;
    align-items: center;
    gap: 20px;
    min-height: 38px;
    padding: 0 10px;
    color: #806344;
    font-size: 8px;
    font-weight: 700;
    letter-spacing: 0.1em;
  }

  .video-topbar > span:last-child {
    text-align: right;
  }

  .video-dots {
    display: flex;
    gap: 5px;
  }

  .video-dots span {
    width: 6px;
    height: 6px;
    border: 1px solid #b9ad9e;
    border-radius: 50%;
  }

  .video-frame video {
    width: 100%;
    aspect-ratio: 16 / 9;
    background: #201d18;
    object-fit: contain;
  }

  .video-caption {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 25px;
    margin-top: 15px;
  }

  .video-caption span {
    color: #806344;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .video-caption p {
    margin: 0;
    color: #625a50;
    font-size: 11px;
  }

  /* =========================
     SYSTEM FLOW
     ========================= */

  .flow {
    position: relative;
    display: grid;
    grid-template-columns:
      1fr 70px 1fr 70px 1fr;
    align-items: center;
    gap: 0;
  }

  .flow-line {
    position: absolute;
    top: 50%;
    left: 12%;
    right: 12%;
    height: 1px;
    background:
      rgba(185, 154, 119, 0.25);
    pointer-events: none;
  }

  .flow-step {
    position: relative;
    z-index: 2;
    min-height: 310px;
    padding: 30px;
    border: 1px solid
      rgba(244, 239, 230, 0.14);
    background: #322e27;
    transition:
      transform 0.3s ease,
      border-color 0.3s ease,
      box-shadow 0.3s ease;
  }

  .flow-step:hover {
    transform: translateY(-8px);
    border-color:
      rgba(185, 154, 119, 0.5);
    box-shadow:
      0 25px 50px
        rgba(0, 0, 0, 0.15);
  }

  .flow-step > span {
    color: #9b7653;
    font-size: 10px;
    font-weight: 700;
  }

  .flow-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 65px;
    height: 65px;
    margin: 35px 0 25px;
    border: 1px solid
      rgba(185, 154, 119, 0.35);
    border-radius: 15px;
    color: #b99a77;
    font-family: monospace;
    font-size: 11px;
    font-weight: 700;
  }

  .flow-step h3 {
    margin: 0 0 10px;
    font-size: 21px;
    font-weight: 600;
  }

  .flow-step p {
    max-width: 280px;
    margin: 0;
    color: #b9b0a3;
    font-size: 12px;
    line-height: 1.7;
  }

  .flow-connector {
    position: relative;
    z-index: 3;
    display: flex;
    justify-content: center;
    color: #9b7653;
    font-size: 22px;
  }

  /* =========================
     TAKEAWAY
     ========================= */

  .takeaway {
    display: grid;
    grid-template-columns:
      100px 1fr;
    gap: 45px;
    max-width: 1000px;
    margin: 0 auto;
  }

  .takeaway-number {
    color: #9b7653;
    font-size: 12px;
    font-weight: 700;
  }

  .takeaway-content h2 {
    max-width: 850px;
    margin: 0 0 25px;
    font-size:
      clamp(42px, 5vw, 65px);
    line-height: 1;
    letter-spacing: -0.055em;
    font-weight: 600;
  }

  .takeaway-content h2 span {
    color: #9b7653;
  }

  .takeaway-content > p {
    max-width: 760px;
    margin: 0 0 18px;
    color: #625a50;
    font-size: 14px;
    line-height: 1.75;
  }

  .takeaway-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    margin-top: 30px;
  }

  .takeaway-tags span {
    padding: 6px 10px;
    border: 1px solid #d2c5b6;
    color: #665e53;
    font-size: 10px;
  }

  /* =========================
     PROJECT INFO
     ========================= */

  .project-info-section {
    padding: 55px 0;
  }

  .project-info-grid {
    display: grid;
    grid-template-columns:
      1.5fr 0.6fr 1fr;
    gap: 40px;
    align-items: start;
  }

  .project-info-grid > div {
    padding-right: 30px;
    border-right: 1px solid
      rgba(244, 239, 230, 0.12);
  }

  .project-info-grid > div:last-child {
    border-right: 0;
  }

  .project-info-grid span {
    display: block;
    margin-bottom: 9px;
    color: #9b7653;
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  .project-info-grid h3 {
    margin: 0;
    font-size: 17px;
    font-weight: 600;
  }

  .project-info-grid p {
    margin: 0;
    color: #b9b0a3;
    font-size: 12px;
    line-height: 1.6;
  }

  /* =========================
     PROJECT END
     ========================= */

  .project-end {
    text-align: center;
  }

  .project-end h2 {
    margin: 0 0 35px;
    font-size:
      clamp(50px, 6vw, 76px);
    line-height: 0.98;
    letter-spacing: -0.06em;
    font-weight: 600;
  }

  .project-end h2 span {
    color: #9b7653;
  }

  /* =========================
     INTERACTIVE GLOW
     ========================= */

  .interactive-card::before {
    content: "";
    position: absolute;
    inset: 0;
    background:
      radial-gradient(
        circle at
          var(--mouse-x, 50%)
          var(--mouse-y, 50%),
        rgba(185, 154, 119, 0.1),
        transparent 30%
      );
    opacity: 0;
    pointer-events: none;
    transition:
      opacity 0.3s ease;
  }

  .interactive-card:hover::before {
    opacity: 1;
  }

  /* =========================
     REVEAL
     ========================= */

  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition:
      opacity 0.75s ease,
      transform
        0.75s cubic-bezier(
          0.2,
          0.7,
          0.2,
          1
        );
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .overview-stat:nth-child(2).reveal {
    transition-delay: 0.08s;
  }

  .overview-stat:nth-child(3).reveal {
    transition-delay: 0.16s;
  }

  .technology-card:nth-child(2).reveal {
    transition-delay: 0.07s;
  }

  .technology-card:nth-child(3).reveal {
    transition-delay: 0.14s;
  }

  .technology-card:nth-child(4).reveal {
    transition-delay: 0.21s;
  }

  .technology-card:nth-child(5).reveal {
    transition-delay: 0.28s;
  }

  .role-card:nth-child(2).reveal {
    transition-delay: 0.1s;
  }

  .role-card:nth-child(3).reveal {
    transition-delay: 0.2s;
  }

  /* =========================
     FOOTER
     ========================= */

  .footer {
    background: #201d18;
    color: #8f877c;
  }

  .footer-container {
    width:
      min(1180px, calc(100% - 64px));
    min-height: 80px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    font-size: 10px;
  }

  /* =========================
     RESPONSIVE
     ========================= */

  @media (max-width: 1050px) {
    .technology-grid {
      grid-template-columns:
        repeat(3, 1fr);
    }

    .technology-card:nth-child(4),
    .technology-card:nth-child(5) {
      min-height: 290px;
    }

    .project-hero-grid {
      gap: 60px;
    }

    .feature-group {
      grid-template-columns:
        220px 1fr;
      gap: 40px;
    }
  }

  @media (max-width: 850px) {
    .project-hero-grid,
    .overview-grid,
    .interface-showcase {
      grid-template-columns: 1fr;
    }

    .project-hero-preview {
      max-width: 650px;
    }

    .overview-main {
      min-height: auto;
    }

    .overview-side {
      grid-template-columns:
        repeat(3, 1fr);
    }

    .roles-grid {
      grid-template-columns: 1fr;
    }

    .role-card {
      min-height: 360px;
    }

    .feature-group {
      grid-template-columns: 1fr;
      gap: 30px;
    }

    .flow {
      grid-template-columns: 1fr;
      gap: 15px;
    }

    .flow-line {
      display: none;
    }

    .flow-connector {
      transform: rotate(90deg);
    }

    .flow-step {
      min-height: 260px;
    }

    .project-info-grid {
      grid-template-columns: 1fr;
      gap: 25px;
    }

    .project-info-grid > div {
      padding-right: 0;
      padding-bottom: 25px;
      border-right: 0;
      border-bottom: 1px solid
        rgba(244, 239, 230, 0.12);
    }

    .project-info-grid > div:last-child {
      border-bottom: 0;
      padding-bottom: 0;
    }
  }

  @media (max-width: 750px) {
    .section {
      padding: 90px 0;
    }

    .section-container,
    .footer-container {
      width:
        min(100% - 40px, 600px);
    }

    .project-hero {
      min-height: auto;
      padding: 105px 0 90px;
    }

    .project-back {
      margin-bottom: 40px;
    }

    .project-hero-content h1 {
      font-size:
        clamp(48px, 13vw, 70px);
    }

    .project-hero-description {
      font-size: 15px;
    }

    .project-quick-info {
      gap: 25px;
      flex-wrap: wrap;
    }

    .technology-grid {
      grid-template-columns:
        repeat(2, 1fr);
    }

    .overview-side {
      grid-template-columns: 1fr;
    }

    .overview-main {
      grid-template-columns: 1fr;
      gap: 20px;
      padding: 30px;
    }

    .feature-items {
      grid-template-columns: 1fr;
    }

    .interface-side {
      grid-template-columns: 1fr;
    }

    .video-caption {
      align-items: flex-start;
      flex-direction: column;
      gap: 5px;
    }

    .takeaway {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .footer-container {
      min-height: 100px;
      align-items: flex-start;
      flex-direction: column;
      justify-content: center;
    }
  }

  @media (max-width: 480px) {
    .section {
      padding: 75px 0;
    }

    .section-container,
    .footer-container {
      width: calc(100% - 32px);
    }

    .project-hero {
      padding-top: 95px;
    }

    .project-back {
      margin-bottom: 35px;
    }

    .project-hero-content h1 {
      font-size: 48px;
    }

    .project-hero-buttons {
      align-items: stretch;
      flex-direction: column;
    }

    .primary-button,
    .secondary-button {
      width: 100%;
    }

    .project-quick-info {
      display: grid;
      grid-template-columns:
        repeat(2, 1fr);
    }

    .project-hero-preview {
      padding: 8px;
    }

    .preview-label {
      left: 18px;
      bottom: 18px;
    }

    .technology-grid {
      grid-template-columns: 1fr;
    }

    .technology-card {
      min-height: 280px;
    }

    .role-card {
      min-height: 390px;
      padding: 27px;
    }

    .role-divider {
      left: 27px;
      right: 27px;
      bottom: 105px;
    }

    .role-functions {
      left: 27px;
      right: 27px;
      bottom: 27px;
    }

    .feature-group-heading {
      grid-template-columns:
        30px 1fr;
      gap: 12px;
    }

    .feature-group-heading h3 {
      font-size: 21px;
    }

    .feature-item {
      min-height: 135px;
    }

    .interface-caption {
      align-items: flex-start;
      flex-direction: column;
      gap: 10px;
    }

    .video-topbar {
      grid-template-columns:
        auto 1fr auto;
      font-size: 7px;
    }

    .takeaway-content h2 {
      font-size: 45px;
    }

    .project-end h2 {
      font-size: 50px;
    }
  }

  /* =========================
     REDUCED MOTION
     ========================= */

  @media (prefers-reduced-motion: reduce) {
    :global(html) {
      scroll-behavior: auto;
    }

    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }

    .reveal,
    .reveal.visible {
      opacity: 1;
      transform: none !important;
    }

    .project-eyebrow,
    .project-hero-content h1,
    .project-hero-description,
    .project-hero-buttons,
    .project-quick-info,
    .project-hero-preview {
      opacity: 1;
      transform: none !important;
    }
  }
</style>